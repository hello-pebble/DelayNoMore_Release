# DelayNoMore — 대화형 투두리스트 생성 데모

**🔗 라이브 데모: <http://delaynomoreapp.duckdns.org/>**

> 현재 버전: **v0.18.0**

### 최근 변경 — v0.17.0 → v0.18.0

| 영역 | 이전 (v0.17.0) | 현재 (v0.18.0) |
| :--- | :--- | :--- |
| 화면 구조 | 가로 3칸 그리드(대화·오늘·체크리스트)가 기본, 760px 이하에서만 위아래 스택 | **모바일 전용 단일 화면** — 한 번에 한 패널이 세로 전체를 쓰고, **하단 고정 탭바**로 전환 |
| 넓은 화면 | 폭 전체를 3칸이 나눠 씀 | 같은 UI를 **폰 폭(최대 480px) 컬럼**으로 가운데 정렬 — 데스크톱 전용 분기 없음(유지보수 대상 레이아웃 1개) |
| 패널 전환 | (해당 없음) | 비활성 패널은 **언마운트하지 않고 숨김** — 대화 스크롤 위치와 진행 중인 SSE 스트리밍이 끊기지 않는다. 오늘 탭에 미완료 개수 배지 |
| 모바일 뷰포트 | `#root: 100vh` — iOS 주소창 때문에 입력바·하단 UI가 화면 밖으로 밀림 | **`100dvh`** + `viewport-fit=cover` + `env(safe-area-inset-bottom)`(홈 인디케이터가 탭바를 덮지 않음) |
| 터치 조작 | 입력 폰트 14px(iOS 포커스 시 화면 확대), 전송 42px, 체크박스 14~15px | 입력 **16px 고정**, 전송 **44×44**, 체크박스 18px + 행 패딩, 버튼 최소 높이 38px, 계획 동작 바 2열 그리드 |
| 안내 문구 | 52px 헤더에 긴 데모 안내 + AI 상태 텍스트를 함께 배치(좁은 화면에선 안내를 숨김) | 상태 LED를 누르면 펼쳐지는 **배너로 이동**(문구 유지). "왼쪽 대화 / 오른쪽 체크리스트" → **"대화 탭 / 체크리스트 탭"** |

프레젠테이션 계층만 바뀌었다 — 백엔드·대화 로직·서버 프롬프트는 변경 없음.
전체 이력은 [CHANGELOG.md](CHANGELOG.md), 실측 수치는 [docs/QA_RESULT_v0.16.0.md](docs/QA_RESULT_v0.16.0.md) 참고.

AI 코치와의 대화로 하루 단위 계획(체크리스트)을 만들고 → 오늘 할 일을 실행하고 → "오늘 마무리" 회고로 하루를 닫습니다.
코치는 v0.15.0부터 **도구를 호출하는 에이전트**입니다 — 완료율·회고 같은 사실을 추측하지 않고 서버 도구로 확인하고, 계획 수정도 도구로 실행합니다.
최종 목표는 **계획을 고정하는 순간, 체크리스트 완성 도우미 대신 그 목표의 전문 에이전트가 이어받는 것**입니다 —
v0.17.0에서 그 1단계에 도달했습니다: 계획을 고정하면 **목표명으로 특화된 전문 에이전트 프로필**(시스템 프롬프트 + 도구 집합)이 대화를 이어받습니다.

## 핵심 기능

- **슬롯필링 대화 → 계획 생성** — 목표·기간·시간·수준을 묻고 하루 단위 투두리스트 초안을 생성(SSE 실시간 스트리밍, API 키 없으면 mock 폴백).
- **자유 대화로 계획 보완 + 상태 수명주기** — 수정 요청·질문을 LLM이 구분해 처리하고, "계획 저장"으로 고정(확정)하면 수정 없이 실행에 집중. 계획은 `초안(DRAFT) → 고정(CONFIRMED) → 완료(COMPLETED)`(또는 중단)의 서버 전이표를 따르며, 상태를 다루는 버튼(저장·기간 연장·중단·다시 만들기)은 체크리스트 패널의 **계획 동작 바**에 있습니다(대화창에는 상태 안내 칩만).
- **여러 계획 보관 + 오늘 할 일** — 계획은 서버 보관함에 자동 저장되고, **오늘 탭**에 모든 계획의 오늘 항목이 모입니다. 배포 환경(`postgres` 프로필)은 PostgreSQL에 영속되어 서버 재시작에도 유지되고, 로컬/단위 테스트 기본값은 인메모리(휘발성)입니다.
- **브라우저별 개인 보관함** — 브라우저가 생성한 게스트 ID로 계획·회고·이력이 격리됩니다(닉네임은 표시용 라벨). 로그인 전 브라우저 단위 임시 보관함이라, DB에 데이터가 남아 있어도 **브라우저의 게스트 ID를 잃으면**(브라우저 데이터 삭제·다른 브라우저/기기 접속) 닉네임만으로는 재연결할 수 없습니다.
- **일일 회고 → 전체 계획 완료 연동** — 자동 계산된 완료율에 체감 난이도·이유를 더해 하루를 마무리하면 해당 Day 카드에 "오늘 마무리 완료" 배지가 붙습니다(오늘 계획 완료). 마무리 시점(종료일 도달 또는 100% 완료)의 회고 저장은 **전체 계획 완료(종결)** 처리를 함께 제안합니다 — 일 단위 마무리와 계획 종결을 구분해 표시.
- **미완료 내일로 이동 + 지난 날짜 잠금** — 오늘 못 끝낸 항목을 버튼 하나로 내일로 이월(종결 전 계획, 고정 후에도 가능). 고정된 계획의 지난 날짜 완료 체크는 소급 변경할 수 없습니다(미루지 않은 지난 항목은 놓친 것으로 확정).
- **계획 변경 이력** — 생성·수정·고정·완료 체크·회고·삭제가 언제, 어느 세션에서 일어났는지 계획별로 조회.
- **다음 계획 분량 추천** — 완료했거나 3일 이상 실행한 계획에서 서버가 수행 기록을 계산하고 규칙으로 다음 하루 분량을 제안합니다(AI는 이유·내용만, 승인 전 미저장). 같은 목표의 최근 계획을 최대 3건 합산해 통계를 안정화합니다.
- **AI 코치의 에이전트화(도구 호출)** — 코치가 답하기 전에 서버 도구를 호출합니다(오늘 할 일·주간 완료율·회고 기록·분량 추천 조회 + 계획 수정·미완료 이월). **어떤 도구를 모델에게 노출할지는 계획 상태(`PlanStatus`)가 결정**하므로, 고정된 계획에는 수정 도구가 프롬프트에 실리지 않습니다 — 프롬프트로 부탁하는 것이 아니라 구조적으로 수정이 불가능합니다. 말풍선 위의 **실행 추적 패널**에서 호출한 도구와 서버가 돌려준 결과를 펼쳐 볼 수 있고, 도구를 지원하지 않는 모델에서는 기존 대화 경로로 자동 폴백합니다.
- **상태별 에이전트 프로필(v0.17.0)** — 상태는 도구만 고르는 것이 아니라 **누가 응대하는지**도 고릅니다: 초안은 체크리스트 완성 코치, 고정하면 **목표명으로 특화된 전문 에이전트**(예: "정보처리기사 실기 전문 에이전트"), 종결(완료/중단)되면 돌아보기와 다음 계획 준비를 돕는 **회고 도우미**. 대화 패널 헤더와 실행 추적 패널에 현재 프로필이 표시됩니다.
- **모바일 전용 화면(v0.18.0)** — 한 번에 한 패널(대화 / 오늘 / 체크리스트)이 세로 전체를 쓰고, **하단 고정 탭바**로 전환합니다. 데스크톱에서 접속해도 같은 UI가 폰 폭(최대 480px) 컬럼으로 가운데 정렬돼, 넓은 화면 전용 레이아웃은 존재하지 않습니다. 탭을 옮겨도 비활성 패널은 언마운트되지 않아 대화 스크롤과 진행 중인 스트리밍이 유지됩니다.

## 빠른 시작

```bash
# 백엔드 (포트 8080)
cd backend && OPENROUTER_API_KEY=<your_key> ./gradlew bootRun

# 프론트엔드 (포트 5173)
cd frontend && npm install && npm run dev
```

또는 단일 컨테이너로:

```bash
docker build -t delaynomore . && docker run -p 8080:8080 -e OPENROUTER_API_KEY=<your_key> delaynomore
```

`OPENROUTER_API_KEY`가 없어도 기동되며 mock 폴백으로 동작합니다. 자세한 내용은 [실행·배포 가이드](docs/DEPLOY.md)를 보세요.

## 문서

| 문서 | 내용 |
| :--- | :--- |
| [기능 상세](docs/FEATURES.md) | 화면 구성과 13가지 기능의 상세 동작 |
| [에이전트](docs/AGENT.md) | 도구 카탈로그 · 상태 기반 권한 모델 · 루프와 폴백 체인 · 토큰 사용량 계측 |
| [평가 하네스](docs/EVAL.md) | 실제 모델로 도구 선택 정확도 측정 · 채점 기준 · 정확도와 비용을 한 표에 |
| [구조](docs/ARCHITECTURE.md) | 디렉토리 구조 · 기술 스택 · API 개요 |
| [발전 과정](docs/EVOLUTION.md) | v0.1.0부터의 버전별 다이어그램·의도 · 토큰 절감 증빙 · 버전 관리 정책 |
| [로드맵](docs/ROADMAP.md) | 다음 단계 6가지와 최종 목표(전문 에이전트 인계) |
| [실행·배포](docs/DEPLOY.md) | 로컬 실행 · 단일 컨테이너 배포 · 환경변수 |
| [CHANGELOG](CHANGELOG.md) | 버전별 상세 변경 이력 |
