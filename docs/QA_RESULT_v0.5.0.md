# QA 수행 결과 — v0.5.0 (오늘 할 일)

- **수행일**: 2026-07-17
- **대상**: `claude/session-2k87x8` 브랜치 (v0.5.0 커밋)
- **환경**: 로컬 — Spring Boot 백엔드(:8080) + Vite 개발 서버(`/api` 프록시) + Chromium 헤드리스(Playwright) 자동 검증. AI 키 미설정(mock 폴백 경로).
- **기준 문서**: [`QA_CHECKLIST.md`](./QA_CHECKLIST.md) — 신규 **F-6** 전 항목 + v0.4.1 기능 회귀(F-3·F-4·F-5 핵심).
- **결과 요약**: 자동 검증 **31개 항목 전부 PASS** (아래 표). 배포 환경 전용 항목만 배포 후 수동 확인 필요.

## F-6. 오늘 할 일 / 오늘 보기 (신규 — 18개 자동 검증)

| 항목 | 결과 | 비고 |
| :--- | :---: | :--- |
| 계획 2개(하나는 고정) 보관 시 밴드에 두 계획명 병합 표시 | ✅ | DRAFT + CONFIRMED 시딩 |
| "오늘 n/m 완료" 진행률 표시 | ✅ | 0/5로 시작 |
| 오늘 항목만 표시(내일 날짜 항목 제외) | ✅ | |
| 고정 계획 그룹에 🔒 아이콘 | ✅ | |
| 비활성 계획 체크 → 서버 즉시 반영(PUT) | ✅ | `GET /plans/{id}` 재조회로 확인 |
| 토글 후 계획 `status` 보존 | ✅ | DRAFT 유지 |
| 체크 즉시 진행률 갱신(0/5 → 1/5) | ✅ | |
| **새로고침 후에도 완료 상태 유지** | ✅ | 서버 저장 → 다른 기기에서도 동일 |
| 고정 계획도 밴드에서 체크 가능 + CONFIRMED 유지 | ✅ | 고정과 충돌 없음 |
| "계획 보기" → 해당 계획으로 전환 + "보는 중" 표시 | ✅ | 기존 보관함 전환 재사용 |
| 활성 계획이 밴드+우측에 각 1회만 표시(이중 표기 없음) | ✅ | |
| 밴드 체크 → 우측 체크리스트 **즉시** 반영 | ✅ | 150ms 내(디바운스 무대기) |
| 우측 체크 → 밴드 **즉시** 반영(양방향) | ✅ | 150ms 내 |
| 활성 계획 토글도 서버 동기화(600ms 디바운스 후) | ✅ | |
| 계획 삭제 → 밴드에서 해당 그룹 소멸 | ✅ | |
| 오늘 항목 없으면 "오늘 할 일이 없습니다" | ✅ | 미래 시작 계획만 남긴 상태 |
| 밴드 접기/펼치기 동작(펼칠 때 재조회) | ✅ | |
| 모바일 폭(400px)에서 밴드가 분할 화면 위 | ✅ | band.y=52 < split.y=116 |

## 회귀 — v0.4.1 기능 (13개 자동 검증)

| 영역 | 항목 | 결과 |
| :--- | :--- | :---: |
| F-3 접근성 | 체크리스트가 실제 체크박스로 렌더링 | ✅ |
| F-3 접근성 | 체크박스 `Tab` 포커스 가능 | ✅ |
| F-3 접근성 | `Space`로 완료 토글 | ✅ |
| F-3 내보내기 | "복사" → "복사됨" + 클립보드에 계획 텍스트 | ✅ |
| F-3 내보내기 | "다운로드" → 내용에 목표/Day 포함 | ✅ * |
| F-4 고정 | "계획 저장(고정)" → 고정됨 배지 | ✅ |
| F-4 고정 | 고정 후 `주말은 빼줘` → AI 호출 없이 🔒 안내 **1회** | ✅ |
| F-4 고정 | 차단 시 "분석 중" 표시 없음 | ✅ |
| F-5 보관함 | 목록 펼친 채 체크 → 본문·"보는 중" 행 진행률 즉시 동일 | ✅ |
| F-5 보관함 | 본문 진행 바 라이브 갱신 | ✅ |
| E 생성 | 슬롯필링 → 체크리스트 생성(mock 폴백 경로) | ✅ |
| A 레이아웃 | 좌우 분할 + 상단 밴드 공존 | ✅ |
| G 반응형 | 좁은 폭에서 상하 스택 + 밴드 상단 유지 | ✅ |

\* 다운로드 **파일명**(`delaynomore-<목표>.txt`)은 헤드리스 Playwright가 프로그래매틱 blob 다운로드의
`a.download` 속성을 `suggestedFilename()`으로 노출하지 않아 자동 검증 불가(v0.4.1에서 원인 확인,
코드는 정상). 일반 Chrome에서 파일명 수동 확인 권장.

## 미실시 — 배포 환경 전용(수동 확인 필요)

- B. AI 연결 상태 LED(키 설정/미설정 표시) — 키 미설정 경로만 확인됨
- F/F-2. 실제 LLM 응답 품질(키 연결 상태의 수정/질문/불명확 응답) — 이번 릴리스 변경 없음
- F-5/F-6 중 "**다른 브라우저/기기**에서 반영" — 서버 저장은 확인했으므로 동등 검증됨(동일 서버 재조회). 배포 후 실기기 교차 확인 권장
- H. 배포/운영(도커 기동·재부팅·health) — 배포 후 확인
