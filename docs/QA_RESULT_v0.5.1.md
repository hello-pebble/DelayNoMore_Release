# QA 수행 결과 — v0.5.1 (세로 3단 스택 레이아웃)

- **수행일**: 2026-07-17
- **대상**: `claude/session-2k87x8` 브랜치 (v0.5.1 커밋)
- **환경**: 로컬 — Spring Boot 백엔드(:8080) + Vite 개발 서버(`/api` 프록시) + Chromium 헤드리스(Playwright) 자동 검증
- **변경 성격**: 레이아웃만 변경(좌우 분할 → 세로 3단: 대화/오늘 할 일/체크리스트). 기능 로직 무변경 → v0.5.0의 F-6 기능 검증을 새 레이아웃에서 전량 재실행해 회귀 확인.
- **결과 요약**: 자동 검증 **19개 항목 전부 PASS**.

## 검증 항목

| 항목 | 결과 | 비고 |
| :--- | :---: | :--- |
| **데스크톱(1280px): 대화 → 밴드 → 체크리스트 세로 순서** | ✅ | v0.5.1 신규 |
| **모바일(400px): 동일한 세로 순서 유지(미디어쿼리 제거 후)** | ✅ | v0.5.1 신규 |
| 두 계획(DRAFT+CONFIRMED) 오늘 항목 병합 표시 | ✅ | F-6 회귀 |
| "오늘 n/m 완료" 진행률 표시·즉시 갱신 | ✅ | F-6 회귀 |
| 오늘 항목만 필터(내일 항목 제외) | ✅ | F-6 회귀 |
| 고정 계획 🔒 표시 + 체크 가능 + CONFIRMED 유지 | ✅ | F-6 회귀 |
| 비활성 계획 체크 → 서버 즉시 PUT + `status` 보존 | ✅ | F-6 회귀 |
| 새로고침 후 완료 상태 유지 | ✅ | F-6 회귀 |
| "계획 보기" → 계획 전환 + "보는 중" 표시 | ✅ | F-6 회귀 |
| 활성 계획 밴드+체크리스트 각 1회 표시(이중 아님) | ✅ | F-6 회귀 |
| 밴드 체크 ↔ 아래 체크리스트 양방향 즉시 반영 | ✅ | F-6 회귀 |
| 활성 계획 토글 서버 동기화(디바운스 후) | ✅ | F-6 회귀 |
| 계획 삭제 → 밴드 그룹 소멸 | ✅ | F-6 회귀 |
| 오늘 항목 없음 → "오늘 할 일이 없습니다" | ✅ | F-6 회귀 |
| 밴드 접기/펼치기 | ✅ | F-6 회귀 |

(양방향 반영·서버 반영 계열은 표에서 묶어 표기 — 스크립트 기준 총 19개 체크)

빌드 검증: ESLint · Vite 프로덕션 빌드 통과. 화면 안내 문구의 방향 표현(왼쪽/오른쪽 → 위/아래) 치환 후 소스에 좌우 방향 표현이 남지 않음을 grep으로 확인.
