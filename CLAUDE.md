# CLAUDE.md — 이 저장소에서 작업할 때의 규칙

## 릴리스 규칙

버전을 올려 릴리스할 때는 코드·CHANGELOG 외에 아래 두 가지를 **반드시 함께** 한다:

1. **QA 리스트 생성** — 릴리스되는 버전의 기능 점검 항목을 `docs/QA_CHECKLIST.md`에 새 절
   (`F-NN. <기능명> (vX.Y.Z)`)로 추가한다. 사람이 배포 화면에서 순서대로 확인할 수 있는
   체크박스 목록이어야 하고, 서버 전용 조건은 curl 예시를 붙인다.
2. **README 최신화 + 버전 변경 표** — `README.md`의 "현재 버전"을 올리고, **"최근 변경"
   표**(이전 릴리스 대비 무엇이 바뀌었는지)를 갱신한다. 표는 직전 릴리스와의 비교만 유지하고
   (누적하지 않음), 전체 이력은 CHANGELOG.md 링크로 넘긴다.

기존 관례(계속 유지): 버전 문자열은 `backend/build.gradle` · `frontend/package.json` ·
`README.md` 세 곳을 함께 올린다. CHANGELOG는 최신 우선 `## [X.Y.Z]` + 볼드 테제 문단.
문서·커밋 메시지는 한국어.

## 기타 관례

- 프롬프트 문구는 실측(평가 하네스, `docs/EVAL.md`) 근거와 함께 바꾼다 — 근거 없는 프롬프트
  수정 금지. 판정 기준은 실행 전에 정하고 사후에 바꾸지 않는다.
- 도구 권한의 소스오브트루스는 `PlanStatus` 능력 플래그 하나다 — 다른 곳(프로필·프롬프트·
  프론트)에 권한 표를 재선언하지 않는다.
- `OPENROUTER_API_KEY`는 서버 전용 — 코드·문서·커밋에 절대 넣지 않는다.
